using System;
using System.Collections.Generic;
using System.Drawing;
using System.IO;
using System.Linq;
using System.Net.Mime;
using System.Text;
using System.Threading.Tasks;
using System.Windows.Forms;
using EfficientSegmentation;

namespace Make3DApplication
{
    public class Program
    {
        static void Main(string[] args)
        {
            //Bitmap testBitmap = new SegmentImage(0.5, 500, 20).ExecuteEfficientSegmentation(new Bitmap("inputimage.jpg"), String.Empty);
            //Application.EnableVisualStyles();
            //Application.SetCompatibleTextRenderingDefault(false);
            //Application.Run(new FormForTesting());
            //тестирующий код лежит внутри единственного обработчика события

            //Bitmap bitmap = new Bitmap("\\..\\..\\resources\\input.jpg");

            //string folder = Path.GetPathRoot(Path.GetFullPath("Debug"));
            //folder = folder.Substring(0, folder.LastIndexOf(@"\"));
            //folder = folder.Substring(0, folder.LastIndexOf(@"\"));
            //folder = folder.Substring(0, folder.LastIndexOf(@"\"));



            Console.ReadLine();
        }


    }
}
